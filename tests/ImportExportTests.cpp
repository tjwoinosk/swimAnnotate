#include "gtest/gtest.h"
#include "ImportData.h"
#include "ExportData.h"


TEST(ExportDataTests, OpenCloseFileTest)
{
	EXPECT_TRUE(true);
	/*
	ImportData dataIn;

	//EXPECT_TRUE(dataIn.openReadStream());

	poolDataIn = dataIn.readFrameData();
	frameDataIn = dataIn.readPoolData();

	while (!eof) {
		dataPoint = dataIn.readKeyPointData();
		someKeyPointHolder.addKeyPoint(dataPoint);
	}
	*/
}